module kez {
    struct Derp {
        y: u16,
    }

    let mut t: u32 = 42;
    let u: str = "test";
    let v: f64 = 10.0;
    let w: Derp = Derp { y: 31 };
}


// this is a comment
// foo()()[1].(m) <<==

fn main() -> u8 {
    let x = 1;
    let d: kez::Derp = Derp { y: 0 };
    let j = "this";
    let z: u32 = 4;
    let u = &x;
    x && z;
    x += 1;  // panics
    let j = false;
    let y = 0 - -1 + (2 * 3) << (4 / 5);
    let y = x + 1;
    print(x);
    return;
    return x.new();  // emits warning: unreachable code
}

// test
